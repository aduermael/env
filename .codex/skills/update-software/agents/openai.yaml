interface:
  display_name: "Update software"
  short_description: "Run a Grok, Codex, Cursor CLI, or gcloud CLI dev image update flow"
  default_prompt: "Use $update-software to update a repo pin (Grok Build, Codex, Cursor CLI, or gcloud CLI) and open a PR."

policy:
  allow_implicit_invocation: false
