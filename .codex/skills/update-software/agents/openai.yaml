interface:
  display_name: "Update software"
  short_description: "Run a Grok, Codex, Cursor CLI, gcloud CLI, or kubectl dev image update flow"
  default_prompt: "Use $update-software to update a repo pin (Grok Build, Codex, Cursor CLI, gcloud CLI, or kubectl) and open a PR."

policy:
  allow_implicit_invocation: false
